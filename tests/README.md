# tests/

**Responsibility:** Automated tests.

- Unit tests (per-module, isolated)
- Integration tests (future — cross-module, e.g. Queue Engine + Database)
- System tests (future — end-to-end, Phase 11)

**Status (Phase 2):** Contains `test_environment.py` only (Phase 1 smoke
test). Module-specific tests are added alongside each module's
implementation, starting Phase 4/5.
