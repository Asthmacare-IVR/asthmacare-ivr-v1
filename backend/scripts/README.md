# scripts/

**Responsibility:** Development scripts.

- Build helpers
- Maintenance utilities
- One-off operational scripts (e.g. future DB seed/reset helpers)

Not part of the application runtime — these are run manually by developers,
never imported by `api/`, `queue_engine`, `database/`, or `telephony/`.

**Status (Phase 2):** Empty. Populated as tooling needs arise in later
phases.
