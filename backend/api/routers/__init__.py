"""HTTP route modules for the AsthmaCare IVR Platform backend API.

Each module in this package defines one ``fastapi.APIRouter`` scoped to a
single concern (health, version, and further routers added in later
GitHub issues). Routers are registered onto the application in
``backend/api/app.py``.
"""
