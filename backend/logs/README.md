# logs/

**Responsibility:** Runtime log files.

This directory is ignored by Git except for this placeholder, so the folder
exists in a fresh clone without committing actual log data (which could
contain patient-related information and must never enter version control).

**Status (Phase 2):** Empty. Logging configuration is added in `config/`
starting Phase 3 onward, as each layer needs it.
